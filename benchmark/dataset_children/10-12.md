| Question | Ground Truth Answer | Source File |
| --- | --- | --- |
| What are the three distinct steps in the RLHF (Reinforcement Learning from Human Feedback) pipeline? | The three steps are: 1) Collect demonstration data to train a supervised policy (SFT), 2) Collect comparison data to train a reward model, and 3) Optimize the policy against the reward model using reinforcement learning (PPO).

 | cs224n-spr2024-lecture10-prompting-rlhf.pdf |
| How does Direct Preference Optimization (DPO) differ from standard RLHF regarding the reward model? | DPO removes the need for an explicit reward model and instead optimizes the language model parameters directly on preference data by solving a simple binary classification problem.

 | cs224n-spr2024-lecture10-prompting-rlhf.pdf |
| What is "Chain-of-Thought" prompting and what type of tasks does it primarily benefit? | Chain-of-Thought prompting involves asking the model to generate intermediate reasoning steps (e.g., "Let's think step by step") before the final answer, which significantly aids performance on multi-step reasoning tasks like math word problems.

 | cs224n-spr2024-lecture10-prompting-rlhf.pdf |
| What is "Zero-shot" learning in the context of GPT-2? | Zero-shot learning is the ability of a model to perform tasks without any examples or gradient updates by simply specifying the task prediction problem in the prompt.

 | cs224n-spr2024-lecture10-prompting-rlhf.pdf |
| Why are n-gram overlap metrics like BLEU and ROUGE often considered insufficient for evaluating text generation? | These metrics measure lexical similarity and lack a concept of semantic relatedness, meaning a generated response can be semantically correct but receive a low score if it does not share exact words with the reference.

 | cs224n-spr2024-lecture11-evaluation-yann.pdf |
| What is "contamination" in the context of Large Language Model evaluation? | Contamination refers to the issue where benchmarks or test data are included in the model's pretraining data, which can lead to inflated performance scores that do not reflect true generalization.

 | cs224n-spr2024-lecture11-evaluation-yann.pdf |
| What is AlpacaEval and how does it function? | AlpacaEval is an automatic evaluation tool that uses a strong language model (like GPT-4) to estimate the "win rate" of a model's outputs against a baseline, providing a faster and cheaper alternative to human annotation.

 | cs224n-spr2024-lecture11-evaluation-yann.pdf |
| What are the major drawbacks of using human evaluation for text generation benchmarks? | Human evaluation is described as slow, expensive, not reproducible, and prone to issues like inter-annotator disagreement and instability over time.

 | cs224n-spr2024-lecture11-evaluation-yann.pdf |
| Why is gradient scaling required when training with FP16 (half-precision)? | FP16 has a small dynamic range, causing small gradient values to "underflow" (become zero); scaling the loss multiplies the gradients into a representable range to prevent this.

 | cs224n-spr2024-lecture12-training-shikhar.pdf |
| How does ZeRO Stage-1 reduce memory consumption compared to standard Distributed Data Parallel (DDP)? | ZeRO Stage-1 shards the optimizer state (momentum and variance) across GPUs instead of replicating the full state on every device, significantly reducing memory overhead.

 | cs224n-spr2024-lecture12-training-shikhar.pdf |
| What is the primary advantage of using Bfloat16 over FP16 for training? | Bfloat16 allocates 8 bits to the exponent (matching FP32), which provides a greater dynamic range than FP16 and eliminates the need for gradient scaling to prevent underflow.

 | cs224n-spr2024-lecture12-training-shikhar.pdf |
| How does Low-Rank Adaptation (LoRA) achieve parameter efficiency? | LoRA freezes the pretrained model weights and injects trainable low-rank decomposition matrices (A and B) into layers, meaning only a very small subset of parameters needs to be updated during finetuning.

 | cs224n-spr2024-lecture12-training-shikhar.pdf |