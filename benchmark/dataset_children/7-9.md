| Question | Ground Truth Answer | Source File |
| --- | --- | --- |
| What is the primary metric used to compare machine-written translations to human-written translations in Assignment 3? | The commonest way is BLEU (Bilingual Evaluation Understudy), which computes a similarity score based on the geometric mean of n-gram precision and a brevity penalty. 

 | cs224n-spr2024-lecture07-final-project.pdf |
| What is the "bottleneck problem" in traditional sequence-to-sequence architectures? | The bottleneck problem refers to the requirement that a single fixed-size encoding of the source sentence must capture all information about that sentence for the decoder to use. 

 | cs224n-spr2024-lecture07-final-project.pdf |
| How does attention solve the bottleneck problem in machine translation? | Attention provides a direct connection from each step of the decoder to the encoder, allowing the model to focus on particular parts of the source sequence rather than relying on a single bottleneck vector. 

 | cs224n-spr2024-lecture07-final-project.pdf |
| What are the three steps involved in any variant of the attention mechanism? | The three steps are: 1. Computing attention scores, 2. Taking a softmax to get the attention distribution, and 3. Using that distribution to take a weighted sum of values to obtain the attention output. 

 | cs224n-spr2024-lecture07-final-project.pdf |
| What were the three main desiderata the authors had when designing the Transformer architecture? | The three goals were to: 1. Minimize computational complexity per layer, 2. Minimize path length between any pair of words, and 3. Maximize parallelizable computation. 

 | cs224n-spr2024-lecture08-transformers.pdf |
| Under what condition is the computational complexity per layer lower for a Transformer compared to recurrent models? | The complexity per layer is lower for a Transformer when the sequence length (n) is much smaller than the representation dimension (d). 

 | cs224n-spr2024-lecture08-transformers.pdf |
| According to Kaplan et al. (2020), what is the relationship between model performance and scaling? | Language modeling performance improves smoothly according to a power-law relationship as model size, training data, and compute resources are increased in tandem. 

 | cs224n-spr2024-lecture08-transformers.pdf |
| What did recent research find regarding the performance impact of most Transformer modifications? | Research found that, surprisingly, most modifications to the original Transformer architecture do not meaningfully improve performance. 

 | cs224n-spr2024-lecture08-transformers.pdf |
| What is the dominant modern paradigm for handling a language's vocabulary in NLP? | The dominant paradigm is to learn a vocabulary of subword tokens where each word is split into a sequence of known subwords at training and testing time. 

 | cs224n-spr2024-lecture09-pretraining-updated.pdf |
| How does the Byte-pair encoding (BPE) algorithm build a subword vocabulary? | BPE starts with character-level tokens and iteratively finds and merges the most common adjacent pairs of tokens into new subwords until a desired vocabulary size is reached. 

 | cs224n-spr2024-lecture09-pretraining-updated.pdf |
| What are the three general types of neural architectures used for pretraining mentioned in the lecture? | The three types are Decoders (best for generation), Encoders (good for bidirectional context and text analysis), and Encoder-Decoders. 

 | cs224n-spr2024-lecture09-pretraining-updated.pdf |
| Why is it recommended to avoid using labeled data when scaling foundation models? | Labeled data is avoided because you cannot scale to the size of the internet with it; instead, models use unsupervised learning on diverse, large-scale datasets. 

 | cs224n-spr2024-lecture09-pretraining-updated.pdf |